import 'package:flutter/material.dart';

class ReactPage extends StatelessWidget {
  static const String routeName = '/react';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
